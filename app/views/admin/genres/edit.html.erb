<div class="container">
    <div class="row">

		<h2>ジャンル編集画面</h2>

		<%= form_for [:admin, @genre] do |f| %>
		<label>ジャンル<%= f.text_field :variety %></label>
		<label><%= f.radio_button :genre_status, "有効" %> 有効 </label>
		<label><%= f.radio_button :genre_status, "無効" %> 無効 </label>
		<%= f.submit "変更を保存する" %>
		<% end %>

	</div>
</div>