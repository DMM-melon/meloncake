<h2>配送先編集</h2>

<%= form_for (@delivery) do |f| %>
<label>郵便番号<%= f.text_field :postcode %></label>
<label>住所<%= f.text_field :address %></label>
<label>宛名<%= f.text_field :name %></label>
<%= f.submit "変更を保存する" %>
<% end %>